apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: prodev # ограничиваем namespace
  name: admin
rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]  # все действия (create, update, delete и т.д.)